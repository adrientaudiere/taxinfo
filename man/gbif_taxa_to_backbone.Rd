% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gbif_checklist.R
\name{gbif_taxa_to_backbone}
\alias{gbif_taxa_to_backbone}
\title{Re-express resolved GBIF taxa with GBIF Backbone keys}
\usage{
gbif_taxa_to_backbone(gbif_taxa, verbose = TRUE)
}
\arguments{
\item{gbif_taxa}{(tibble) Resolved taxa with `usageKey` and
`verbatim_name`.}

\item{verbose}{(logical) Report the re-matching and the dropped taxa.}
}
\value{
`gbif_taxa` with GBIF Backbone `usageKey`s (unchanged when its
 keys already are backbone keys).
}
\description{
GBIF SQL downloads filter on the literal `taxonkey` / `specieskey`
occurrence columns, which always hold GBIF Backbone numeric keys. Taxa
resolved in another checklist (COL XR) are therefore re-matched by name in
the GBIF Backbone; taxa without a backbone match are dropped with a
warning.
}
\author{
Adrien Taudiere
}
\keyword{internal}
