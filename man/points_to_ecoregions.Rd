% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/points_to_ecoregions.R
\name{points_to_ecoregions}
\alias{points_to_ecoregions}
\title{Map GPS points to WWF/TNC terrestrial ecoregions}
\usage{
points_to_ecoregions(longitudes, latitudes, ecoregions = NULL)
}
\arguments{
\item{longitudes}{(numeric vector). Longitudes of the points to locate, in
decimal degrees in `[-180, 180]`.}

\item{latitudes}{(numeric vector). Latitudes of the points to locate, in
decimal degrees in `[-90, 90]`. Must have the same length as `longitudes`.}

\item{ecoregions}{(optional `sf` object, default `NULL`). Ecoregion polygon
layer to use. If `NULL`, the shipped WWF/TNC layer is loaded via
[load_ecoregions()] (result is cached, so passing `NULL` is usually the
right choice).}
}
\value{
A tibble with one row per input point and the columns
 `point_id` (integer), `longitude`, `latitude`, `ECO_NAME`, `biome`, `realm`.
 Points falling outside any ecoregion (oceans, poles...) have `NA` in the
 three ecoregion columns.
}
\description{
Assigns each GPS point (pair of longitude/latitude in decimal degrees, WGS84)
to the WWF/TNC terrestrial ecoregion, biome and realm that contains it.
}
\examples{
\dontrun{
points_to_ecoregions(
  longitudes = c(2.3522, 4.2, -70),
  latitudes  = c(48.8566, 33, -33)
)
}
}
\seealso{
[tax_check_ecoregion()], [tax_ecoregion_occur()]
}
\author{
Adrien Taudiere
}
