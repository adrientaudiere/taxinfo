% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gbif_checklist.R
\name{gbif_match_taxa}
\alias{gbif_match_taxa}
\title{Resolve names to GBIF taxa (EXACT or HIGHERRANK matches)}
\usage{
gbif_match_taxa(taxnames, checklist = NULL)
}
\arguments{
\item{taxnames}{(character) Names to match.}

\item{checklist}{Checklist spec, see [gbif_checklist_key()].}
}
\value{
A tibble with columns `usageKey`, `canonicalName` and
 `verbatim_name`.
}
\description{
Runs [gbif_backbone_checklist()] and keeps the `EXACT` / `HIGHERRANK`
matches, one row per (`usageKey`, `canonicalName`, `verbatim_name`). When
no name matches, GBIF returns neither a `usageKey` nor a `canonicalName`
column; the result is then a zero-row tibble with these three columns, so
callers can rely on them, and a warning alert is shown (callers then skip
every GBIF occurrence call and return their usual, empty output).
}
\author{
Adrien Taudiere
}
\keyword{internal}
