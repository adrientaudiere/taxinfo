% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_gbif_occur_coords.R
\name{gbif_occur_coords_search}
\alias{gbif_occur_coords_search}
\title{Per-taxon `occ_search` loop for [tax_gbif_occur_coords()]}
\usage{
gbif_occur_coords_search(
  gbif_taxa,
  n_occur,
  keep_cols,
  verbose = TRUE,
  time_to_sleep = 0.3
)
}
\arguments{
\item{gbif_taxa}{(tibble) Resolved GBIF taxa with `usageKey`, `canonicalName`
and `verbatim_name`.}

\item{n_occur}{(numeric, default `1000`). Maximum number of occurrences to
keep per taxon. With `method = "search"` this is a server-side limit; with
the download methods it is applied as a local sample after import (a warning
is issued when a taxon exceeded `n_occur`).}

\item{keep_cols}{(character) Occurrence columns to retain.}

\item{verbose}{(logical, default `TRUE`). If `TRUE`, print progress messages.}

\item{time_to_sleep}{(numeric, default `0.3`). Seconds to pause between
[rgbif::occ_search()] calls to avoid GBIF rate-limiting. Only used when
`method = "search"`.}
}
\value{
A tibble of occurrences with `taxon_name` and `usageKey`, or `NULL`.
}
\description{
Per-taxon `occ_search` loop for [tax_gbif_occur_coords()]
}
\author{
Adrien Taudiere
}
\keyword{internal}
