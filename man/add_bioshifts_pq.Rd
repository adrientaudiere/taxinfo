% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_bioshifts_pq.R
\name{add_bioshifts_pq}
\alias{add_bioshifts_pq}
\title{Add range-shift information from the BioShifts database to a phyloseq object}
\usage{
add_bioshifts_pq(
  physeq,
  species_rank = "Species",
  genus_rank = "Genus",
  group = "All",
  eco = "All",
  continent = "All",
  type = c("LAT", "ELE"),
  shifts_data = NULL,
  col_prefix = "bioshift_",
  add_to_phyloseq = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{physeq}{(required) A \code{\link[phyloseq]{phyloseq-class}} object with
a `tax_table` containing at least the species rank.}

\item{species_rank}{(character, default `"Species"`) Name of the `tax_table`
column holding the species (or binomial) name.}

\item{genus_rank}{(character, default `"Genus"`) Name of the `tax_table`
column holding the genus name. Used to build the binomial when the species
column holds only the epithet. Ignored if absent from the `tax_table`.}

\item{group, eco, continent, type}{Filters passed to
`BioShiftR::get_shifts()`. `type` is the shift dimension(s) to summarize and
accepts any subset of `c("LAT", "ELE")`. See `?BioShiftR::get_shifts`.}

\item{shifts_data}{(optional data.frame) A pre-fetched
`BioShiftR::get_shifts()` result (with columns `sp_name_checked`, `type`
and `calc_rate`). When supplied, the live query is skipped and `group` /
`eco` / `continent` / `type` are only used to select which `type`s to
summarize. Useful for reproducible analyses and tests.}

\item{col_prefix}{(character, default `"bioshift_"`) Prefix applied to the
added columns (`bioshift_LAT_rate`, `bioshift_ELE_rate`,
`bioshift_n_records`).}

\item{add_to_phyloseq}{(logical, default `TRUE`) If `TRUE`, return the
phyloseq object with the new columns added to its `tax_table`; if `FALSE`,
return the augmented `tax_table` as a tibble.}

\item{verbose}{(logical, default `TRUE`) Print a summary message.}
}
\value{
Either an updated `phyloseq` object (when `add_to_phyloseq = TRUE`)
  or a tibble of the augmented `tax_table` (when `add_to_phyloseq = FALSE`).
}
\description{
<a href="https://adrientaudiere.github.io/taxinfo/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>

Query the BioShifts database (Comte et al. 2024, a global compilation of
documented species range shifts under climate change) through the
`BioShiftR` package and attach, for each taxon of a `phyloseq` object, a
summary of the observed range-shift rates. Latitudinal (`"LAT"`) and
elevational (`"ELE"`) shifts are summarized separately as the mean shift rate
across all matching records, together with the number of records found.

Species are matched by binomial name: the `Genus` and `Species` ranks of the
`tax_table` are combined and compared (case- and separator-insensitively)
with the `sp_name_checked` column returned by `BioShiftR::get_shifts()`.
}
\examples{
\dontrun{
# Live query (needs the BioShiftR package and network access):
data(data_fungi_mini, package = "MiscMetabar")
pq <- add_bioshifts_pq(data_fungi_mini, group = "Plants", type = "LAT")
}

# Offline: supply a pre-fetched shifts table.
shifts <- data.frame(
  sp_name_checked = c("Genusa speciesa", "Genusa speciesa", "Genusb speciesb"),
  type = c("LAT", "LAT", "ELE"),
  calc_rate = c(1.2, 0.8, -3.5),
  stringsAsFactors = FALSE
)
otu <- matrix(1, nrow = 2, ncol = 1, dimnames = list(c("t1", "t2"), "s1"))
tax <- matrix(
  c("Genusa", "speciesa", "Genusb", "speciesb"),
  nrow = 2, byrow = TRUE, dimnames = list(c("t1", "t2"), c("Genus", "Species"))
)
pq <- phyloseq::phyloseq(
  phyloseq::otu_table(otu, taxa_are_rows = TRUE),
  phyloseq::tax_table(tax)
)
add_bioshifts_pq(pq, shifts_data = shifts, add_to_phyloseq = FALSE)
}
\references{
Comte, L. et al. (2024) BioShifts: a global geographical database of
  climate-induced species redistribution over land and sea.
}
\seealso{
\code{\link{add_metatraits_pq}}
}
\author{
Adrien Taudière
}
